---
phase: 1
plan: 3
title: "Data Files & Representatives/States/Promises Sections"
wave: 2
depends_on: [1, 2]
files_modified:
  - data/states-data.js
  - data/mp-data.js
  - data/mla-data.js
  - data/promises-data.js
  - index.html
  - index.css
autonomous: true
requirements_addressed: [REQ-002, REQ-003, REQ-004, REQ-005, REQ-008, REQ-009]
---

# Plan 03: Data Files & Representatives/States/Promises Sections

<objective>
Create all data JS files (MPs, MLAs, states, promises) and build the Representatives section (searchable MP/MLA tables), State Explorer grid, and Promise Tracker section with chart containers in HTML/CSS.
</objective>

<must_haves>
- data/states-data.js with all 28 states + 8 UTs (name, code, loksabhaSeats, vidhanSabhaSeats, rulingParty, capital, type)
- data/mp-data.js with all 543 Lok Sabha MP records (name, constituency, state, party)
- data/mla-data.js with MLA records keyed by state for all states with assemblies (~representative sample per state covering top constituencies)
- data/promises-data.js with categorized promises (Infrastructure, Economy, Welfare, Health, Education, Agriculture) with status (fulfilled/in-progress/not-started)
- Representatives section HTML with tabs for MPs and MLAs, filter bar (state/party dropdowns), search input, and data table container
- States section HTML with grid of state cards
- Promises section HTML with chart canvas containers and category breakdown
- All corresponding CSS for tables, filters, state cards, chart containers
</must_haves>

## Tasks

<task id="3.1">
<title>Create data/states-data.js</title>
<read_first>- (new file)</read_first>
<action>
Create `data/states-data.js` exporting `window.STATES_DATA` array with all 36 states/UTs:
```js
window.STATES_DATA = [
  { name: "Andhra Pradesh", code: "AP", loksabhaSeats: 25, vidhanSabhaSeats: 175, rulingParty: "TDP", capital: "Amaravati", type: "state" },
  { name: "Arunachal Pradesh", code: "AR", loksabhaSeats: 2, vidhanSabhaSeats: 60, rulingParty: "BJP", capital: "Itanagar", type: "state" },
  // ... all 28 states + 8 UTs with accurate seat counts
];
```
Include every state: AP, AR, AS, BR, CG, GA, GJ, HR, HP, JH, KA, KL, MP, MH, MN, ML, MZ, NL, OD, PB, RJ, SK, TN, TS, TR, UP, UK, WB. UTs: AN, CH, DN, DD, DL, GA (already state), JK, LA, LD, PY.
</action>
<acceptance_criteria>
- data/states-data.js exists and contains `window.STATES_DATA`
- File contains entries for "Uttar Pradesh" with vidhanSabhaSeats 403
- File contains entries for "Maharashtra" with vidhanSabhaSeats 288
- File contains at least 36 entries
</acceptance_criteria>
</task>

<task id="3.2">
<title>Create data/mp-data.js with all 543 Lok Sabha MPs</title>
<read_first>- (new file)</read_first>
<action>
Create `data/mp-data.js` with `window.MP_DATA` array of 543 objects. Each: `{ name, constituency, state, party }`. Use actual 18th Lok Sabha MP data. Include all major parties: BJP, INC, SP, TMC, DMK, TDP, JDU, YSRCP, SHS, NCP, AAP, AITC, etc. Keep file under 100KB by using short party abbreviations.
</action>
<acceptance_criteria>
- data/mp-data.js exists and contains `window.MP_DATA`
- Array has 543 entries (verified by `window.MP_DATA.length === 543` comment)
- File contains entries with party "BJP" and party "INC"
- File size under 100KB
</acceptance_criteria>
</task>

<task id="3.3">
<title>Create data/mla-data.js with state-wise MLAs</title>
<read_first>- data/states-data.js (for state names and seat counts)</read_first>
<action>
Create `data/mla-data.js` with `window.MLA_DATA` object keyed by state name. For each state with a Vidhan Sabha, include MLA array: `{ name, constituency, party }`. Include ALL assembly constituencies for ALL 28 states + Delhi, JK, Puducherry. Use representative real MLA data from latest elections. Keep file under 600KB total.
</action>
<acceptance_criteria>
- data/mla-data.js exists and contains `window.MLA_DATA`
- Object has keys for "Uttar Pradesh" with 403 entries
- Object has keys for "Maharashtra" with 288 entries
- Object has keys for at least 28 states
- File size under 600KB
</acceptance_criteria>
</task>

<task id="3.4">
<title>Create data/promises-data.js</title>
<read_first>- (new file)</read_first>
<action>
Create `data/promises-data.js` with `window.PROMISES_DATA` containing categorized government promises with status. Structure:
```js
window.PROMISES_DATA = {
  categories: [
    {
      name: "Infrastructure",
      icon: "road",
      promises: [
        { title: "Bullet Train Corridors", description: "Expand bullet train to North, South, East India", status: "in-progress", year: 2024 },
        { title: "Vande Bharat Expansion", description: "More Vande Bharat trains across India", status: "in-progress", year: 2024 },
        // 5-8 promises per category
      ]
    },
    // Categories: Infrastructure, Economy, Welfare, Health, Education, Agriculture
  ],
  summary: { total: 75, fulfilled: 22, inProgress: 35, notStarted: 18 }
};
```
Use factual, unbiased status assessments based on publicly available data. Include ~75 total promises across 6 categories.
</action>
<acceptance_criteria>
- data/promises-data.js exists and contains `window.PROMISES_DATA`
- Contains categories for "Infrastructure", "Economy", "Welfare", "Health", "Education", "Agriculture"
- Contains `summary` object with total, fulfilled, inProgress, notStarted counts
- Each promise has title, description, status fields
</acceptance_criteria>
</task>

<task id="3.5">
<title>Build Representatives, States, and Promises HTML sections</title>
<read_first>- index.html - index.css</read_first>
<action>
**Representatives section (`#representatives`):** Section header + tab nav (MPs | MLAs) + filter bar (state dropdown, party dropdown, search input) + results count + scrollable data table container (`#repTable`) + pagination controls.

**States section (`#states`):** Section header + grid of state cards (populated by JS) with `#statesGrid` container. Each card template: state name, seats info, ruling party badge.

**Promises section (`#promises`):** Section header + summary stat cards (Total/Fulfilled/In Progress/Not Started) + chart row with two canvas elements (`#barChart` for category breakdown, `#donutChart` for overall status) + category accordion with per-category promise lists and progress bars.

Add CSS for: data table styles, filter bar, pagination, state card grid (auto-fill minmax 250px), chart containers with responsive aspect ratios, promise progress bars, status badges (`.status-fulfilled` green, `.status-in-progress` saffron, `.status-not-started` grey).
</action>
<acceptance_criteria>
- index.html contains `id="representatives"` with filter bar and table container
- index.html contains `id="statesGrid"`
- index.html contains `id="barChart"` and `id="donutChart"` canvas elements
- index.html contains `id="promises"` section with summary cards
- index.css contains `.state-card` and `.promise-progress` or similar
- index.css contains `.status-fulfilled` and `.status-in-progress`
</acceptance_criteria>
</task>

<verification>
1. All 4 data files load without JS errors
2. Representatives section has filter UI and table structure
3. States section has grid container
4. Promises section has chart canvases and category layout
5. Total data files under 700KB combined
</verification>
