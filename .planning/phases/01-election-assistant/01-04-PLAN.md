---
phase: 1
plan: 4
title: "JavaScript — All Interactivity, Charts & Polish"
wave: 3
depends_on: [1, 2, 3]
files_modified:
  - app.js
  - index.css
autonomous: true
requirements_addressed: [REQ-001, REQ-002, REQ-003, REQ-004, REQ-005, REQ-006, REQ-007, REQ-009, REQ-010]
---

# Plan 04: JavaScript — All Interactivity, Charts & Polish

<objective>
Implement all interactive behaviors in app.js: scroll animations, navigation, theme toggle, counter animations, election process tabs, FAQ accordion, quiz engine, MP/MLA table rendering with search/filter/pagination, state explorer grid population, promise tracker canvas charts (bar + donut), global search, and mobile menu.
</objective>

<must_haves>
- IntersectionObserver scroll reveal animations
- Theme toggle with localStorage persistence
- Animated number counters in hero
- Election process tab switching
- FAQ accordion (single-open, keyboard navigation)
- Quiz engine (8 questions, scoring, feedback, restart)
- MP table: render from MP_DATA, search by name/constituency, filter by state/party dropdowns, pagination (20 per page)
- MLA table: select state → render MLAs from MLA_DATA, search/filter by party
- State explorer: populate grid from STATES_DATA, clickable cards showing details
- Bar chart on canvas: 6 categories, stacked bars (fulfilled/in-progress/not-started)
- Donut chart on canvas: overall promise status with animated segments
- Category promise lists with progress bars
- Global search across MPs, MLAs, states, FAQ content
- Smooth scroll navigation with active nav highlighting
- Mobile hamburger menu
</must_haves>

## Tasks

<task id="4.1">
<title>Core utilities, navigation, theme, counters, animations</title>
<read_first>- index.html - index.css</read_first>
<action>
Create `app.js` with DOMContentLoaded init. Implement: initScrollAnimations (IntersectionObserver with .visible class), initHeaderScroll (.scrolled on scroll>50), initSmoothScroll (href="#" with offset), initMobileMenu (toggle .mobile-nav-open), initThemeToggle (data-theme attr + localStorage), initCounters (animate from 0 to data-target with ease-out over 2s using requestAnimationFrame), initNavHighlight (highlight active section on scroll).
</action>
<acceptance_criteria>
- app.js contains IntersectionObserver and `.visible` class toggling
- app.js contains localStorage for theme persistence
- app.js contains `animateCounter` with `requestAnimationFrame`
- app.js contains `initMobileMenu` and `initSmoothScroll`
</acceptance_criteria>
</task>

<task id="4.2">
<title>Election process tabs, FAQ accordion, quiz engine</title>
<read_first>- index.html - app.js (task 4.1)</read_first>
<action>
**Tabs:** Click .tab-btn → toggle .active on btn and corresponding .tab-content.

**FAQ:** Click .faq-question → toggle .active on parent .faq-item, close others, update aria-expanded, animate max-height. Arrow key navigation.

**Quiz:** quizData array with 8 Indian democracy questions. Functions: loadQuestion(index), selectOption (check answer, show green/red, show explanation, reveal Next btn), nextQuestion, showResults (score + message), restartQuiz. Progress bar updates. Results messages: 8/8 "Expert!", 6-7 "Great!", 4-5 "Good effort!", 0-3 "Keep learning!".
</action>
<acceptance_criteria>
- app.js contains tab switching logic
- app.js contains FAQ accordion with `aria-expanded` management
- app.js contains `quizData` array with 8 question objects
- app.js contains `loadQuestion` and `showResults` functions
</acceptance_criteria>
</task>

<task id="4.3">
<title>Representatives table with search, filter, pagination</title>
<read_first>- index.html - data/mp-data.js - data/mla-data.js - app.js</read_first>
<action>
**MP Table:** Render from window.MP_DATA into #repTable. Columns: #, Name, Constituency, State, Party (with party color dot). Populate state and party filter dropdowns dynamically from data. Search input filters by name/constituency (debounced 200ms). Pagination: 20 rows per page, prev/next buttons, page indicator. 

**MLA Table:** When MLA tab active, show state selector dropdown. On state select, render that state's MLAs from window.MLA_DATA[stateName]. Same search/filter/pagination pattern. Show MLA count for selected state.

Both tables: highlight search matches, show "No results" message when empty, smooth fade transition on filter change.
</action>
<acceptance_criteria>
- app.js renders MP data into a table from `window.MP_DATA`
- app.js contains pagination logic with page size 20
- app.js contains debounced search filtering
- app.js populates state and party filter dropdowns dynamically
- app.js renders MLA data from `window.MLA_DATA` on state selection
</acceptance_criteria>
</task>

<task id="4.4">
<title>State explorer grid population</title>
<read_first>- index.html - data/states-data.js - app.js</read_first>
<action>
Populate #statesGrid from window.STATES_DATA. Each state card: glass-card with state name, capital, LS seats, VS seats, ruling party badge (with party color). Cards are searchable. Click card → expand inline detail panel or modal showing full state info. Add filter: type selector (All/States/UTs).
</action>
<acceptance_criteria>
- app.js populates state cards from `window.STATES_DATA`
- app.js creates card elements with state name, seats, ruling party
- app.js supports filtering states/UTs
</acceptance_criteria>
</task>

<task id="4.5">
<title>Promise tracker canvas charts</title>
<read_first>- index.html - data/promises-data.js - app.js</read_first>
<action>
**Bar Chart (#barChart):** Custom canvas rendering. X-axis: 6 categories. Each bar has 3 stacked segments (fulfilled=green, in-progress=saffron, not-started=grey). Y-axis: count. Labels, legend. Animate bars growing from 0 on scroll into view. Responsive: resize on window resize.

**Donut Chart (#donutChart):** Canvas arc segments for fulfilled/in-progress/not-started. Center text: overall percentage. Animate segments drawing clockwise. Color-coded legend.

**Category Breakdown:** Below charts, render each category as a card with: category name, icon, progress bar (fulfilled/total), expandable list of individual promises with status badges.

**Summary Cards:** Populate the 4 summary stat cards (Total, Fulfilled, In Progress, Not Started) from PROMISES_DATA.summary with counter animations.
</action>
<acceptance_criteria>
- app.js contains canvas drawing code for `#barChart`
- app.js contains canvas drawing code for `#donutChart`
- app.js contains `arc` or `fillRect` canvas API calls
- app.js reads from `window.PROMISES_DATA`
- app.js populates promise summary cards
- app.js renders per-category promise lists
</acceptance_criteria>
</task>

<task id="4.6">
<title>Global search and final polish</title>
<read_first>- index.html - index.css - app.js</read_first>
<action>
**Global Search (#globalSearch):** On input (debounced 300ms), search across: MP names/constituencies, MLA names/constituencies, state names, FAQ text, election process text. Show dropdown results panel with categorized results (MPs, MLAs, States, FAQ). Click result → scroll to section and highlight. Escape key closes.

**Polish:** Verify all transitions work, no console errors, theme persists on reload, all charts render correctly, mobile responsive at 375px, keyboard navigation works for all interactive elements. Ensure total repo size stays well under 10MB.

Add CSS: `.search-results-dropdown` glass panel, `.search-result-item` hover states, `.search-category-label` styling.
</action>
<acceptance_criteria>
- app.js contains global search across MPs, MLAs, states, FAQ
- app.js contains search results dropdown rendering
- app.js contains debounce logic for search
- No JavaScript console errors on page load
- Theme toggle persists across reloads
- Total file size (all files) under 1MB (well under 10MB)
</acceptance_criteria>
</task>

<verification>
1. Page loads without errors, mesh gradient animates
2. Hero counters animate to 543/245/28/960
3. Election process tabs switch correctly
4. MP table shows 543 entries, search/filter/pagination work
5. MLA table shows state-specific data on state selection
6. State explorer grid populates with all 36 states/UTs
7. Bar chart and donut chart render and animate on scroll
8. Promise categories show individual promises with status
9. FAQ accordion opens/closes properly
10. Quiz runs through 8 questions with scoring
11. Global search finds MPs, MLAs, states, FAQ items
12. Dark/light theme toggle works and persists
13. Mobile responsive at 375px width
14. Total repo size under 10MB
</verification>
